package org.kleemann.predprey

/**
 * This is a command line entry point into the application.
 * It loads a predefined configuration file that can have ranges of values
 * and runs through each of these scenarios 
 */
object Auto {
  def main(args: Array[String]) {
    println("hello world")
  }
}